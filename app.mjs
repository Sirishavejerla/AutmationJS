import { add, subtract, multiply } from "./mathOperations.mjs";

const a = 10;
const b = 15;

console.log(`Addition of a and b : ${add(a, b)}`);
console.log(`Substraction of a and b : ${subtract(a, b)}`);
console.log(`Multiplication of a and b : ${multiply(a, b)}`);
